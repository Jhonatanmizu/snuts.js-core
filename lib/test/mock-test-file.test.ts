describe("Anonymous Test Suite", () => {
  it("should have anonymous test logic", () => {
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    const a = 1;
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    const b = 2;
    // This is an anonymous test logic smell
  });

  it("another test", () => {
    // Another test
  });
});
